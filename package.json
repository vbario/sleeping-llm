{
  "name": "j",
  "version": "1.0.0",
  "description": "",
  "license": "ISC",
  "author": "",
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "python3 -m src.main --web --port 3000",
    "push-deploy": "git add -A && git commit -m 'Changes' && git push origin memory-version-11"
  }
}
